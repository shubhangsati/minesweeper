class Solver {
    constructor (grid) {
        this.grid = [];
        for (var i = 0; i < grid.length; ++i) {
            var l = [];
            for (var j = 0; j < grid.length; ++j) {
                l.push(0);
            }
            this.grid.push(l);
        }
    }

    solve() {
        
    }
}